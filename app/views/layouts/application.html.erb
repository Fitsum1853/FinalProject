<!DOCTYPE html>
<html>
  <head>
    <title>Myteam</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Correctly include the Bootstrap CSS from CDN -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css", "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <% if coach_signed_in? %>
      <%= button_to "Sign Out", destroy_coach_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign In", new_coach_session_path %>
      <%= link_to "Sign Up", new_coach_registration_path %>
    <% end %>

    <%= yield %>
  </body>
</html>